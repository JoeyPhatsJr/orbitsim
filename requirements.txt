# Orbital Mechanics Simulator — pinned dependencies
# Python 3.11.x required (Panda3D wheels + scipy stability). Do NOT use 3.12+ until verified.

# --- Physics core ---
numpy==1.26.4
scipy==1.13.1
astropy==6.1.0          # constants, time scales (TDB), frame transforms

# --- Real ephemerides (Phase 5) ---
skyfield==1.49          # JPL DE440 planet/moon positions
jplephem==2.22          # kernel reader used by skyfield

# --- Transfers / Lambert (Phase 4) ---
lamberthub==1.0.0a3     # maintained Lambert solver (verify version on install)

# --- Rendering / game shell (Phase 2+) ---
panda3d==1.10.14

# --- Dev / test ---
pytest==8.2.2
pytest-cov==5.0.0
hypothesis==6.103.1     # property-based tests for round-trip invariants

# NOTE: poliastro is intentionally EXCLUDED — it is deprecated/unmaintained.
# We implement the orbital core ourselves (see docs/01-physics-core.md).
